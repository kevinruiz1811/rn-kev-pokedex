import React from "react";
import { Text, SafeAreaView } from "react-native";

const PokedexScreen = () => {
	return (
		<SafeAreaView>
			<Text>Pokedex</Text>
		</SafeAreaView>
	);
};

export default PokedexScreen;
